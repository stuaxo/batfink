{
  "_comment": "zexall sub-tests known to fail on our core: undocumented XF/YF results that need a MEMPTR/WZ model. zexdoc (which masks those bits) passes 100%. A failure outside this list fails the test; a listed test that now passes logs a hint to tighten this.",
  "knownFailing": [
    "bit n,<b,c,d,e,h,l,(hl),a>",
    "cpd<r>",
    "cpi<r>"
  ]
}
