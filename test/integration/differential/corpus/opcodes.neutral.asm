; generated by gen-opcodes.ts — regenerate, do not hand-edit
nn   equ 0x1234
     org 0x8000
     nop
     halt
     di
     ei
     exx
     rlca
     rrca
     rla
     rra
     daa
     cpl
     scf
     ccf
     neg
     retn
     reti
     rrd
     rld
     ldi
     ldd
     ldir
     lddr
     cpi
     cpir
     cpd
     cpdr
     ini
     inir
     ind
     indr
     outi
     otir
     outd
     otdr
     ld b,b
     ld b,c
     ld b,d
     ld b,e
     ld b,h
     ld b,l
     ld b,(hl)
     ld b,a
     ld c,b
     ld c,c
     ld c,d
     ld c,e
     ld c,h
     ld c,l
     ld c,(hl)
     ld c,a
     ld d,b
     ld d,c
     ld d,d
     ld d,e
     ld d,h
     ld d,l
     ld d,(hl)
     ld d,a
     ld e,b
     ld e,c
     ld e,d
     ld e,e
     ld e,h
     ld e,l
     ld e,(hl)
     ld e,a
     ld h,b
     ld h,c
     ld h,d
     ld h,e
     ld h,h
     ld h,l
     ld h,(hl)
     ld h,a
     ld l,b
     ld l,c
     ld l,d
     ld l,e
     ld l,h
     ld l,l
     ld l,(hl)
     ld l,a
     ld (hl),b
     ld (hl),c
     ld (hl),d
     ld (hl),e
     ld (hl),h
     ld (hl),l
     ld (hl),a
     ld a,b
     ld a,c
     ld a,d
     ld a,e
     ld a,h
     ld a,l
     ld a,(hl)
     ld a,a
     ld b,0x2a
     ld c,0x2a
     ld d,0x2a
     ld e,0x2a
     ld h,0x2a
     ld l,0x2a
     ld (hl),0x2a
     ld a,0x2a
     ld a,(bc)
     ld (bc),a
     ld a,(de)
     ld (de),a
     ld a,(nn)
     ld (nn),a
     ld a,i
     ld a,r
     ld i,a
     ld r,a
     ld bc,0x89ab
     ld de,0x89ab
     ld hl,0x89ab
     ld sp,0x89ab
     ld hl,(nn)
     ld (nn),hl
     ld bc,(nn)
     ld (nn),bc
     ld de,(nn)
     ld (nn),de
     ld sp,(nn)
     ld (nn),sp
     ld sp,hl
     ld ix,0x89ab
     ld ix,(nn)
     ld (nn),ix
     ld sp,ix
     ld ixh,0x12
     ld ixl,0x34
     ld ixh,b
     ld c,ixl
     ld iy,0x89ab
     ld iy,(nn)
     ld (nn),iy
     ld sp,iy
     ld iyh,0x12
     ld iyl,0x34
     ld iyh,b
     ld c,iyl
     push bc
     pop bc
     push de
     pop de
     push hl
     pop hl
     push af
     pop af
     push ix
     pop ix
     push iy
     pop iy
     ex af,af'
     ex de,hl
     ex (sp),hl
     ex (sp),ix
     ex (sp),iy
     add a,b
     add a,c
     add a,d
     add a,e
     add a,h
     add a,l
     add a,(hl)
     add a,a
     add a,0x2a
     add a,(ix+5)
     add a,(ix-3)
     add a,ixh
     add a,ixl
     add a,(iy+5)
     add a,(iy-3)
     add a,iyh
     add a,iyl
     adc a,b
     adc a,c
     adc a,d
     adc a,e
     adc a,h
     adc a,l
     adc a,(hl)
     adc a,a
     adc a,0x2a
     adc a,(ix+5)
     adc a,(ix-3)
     adc a,ixh
     adc a,ixl
     adc a,(iy+5)
     adc a,(iy-3)
     adc a,iyh
     adc a,iyl
     sub b
     sub c
     sub d
     sub e
     sub h
     sub l
     sub (hl)
     sub a
     sub 0x2a
     sub (ix+5)
     sub (ix-3)
     sub ixh
     sub ixl
     sub (iy+5)
     sub (iy-3)
     sub iyh
     sub iyl
     sbc a,b
     sbc a,c
     sbc a,d
     sbc a,e
     sbc a,h
     sbc a,l
     sbc a,(hl)
     sbc a,a
     sbc a,0x2a
     sbc a,(ix+5)
     sbc a,(ix-3)
     sbc a,ixh
     sbc a,ixl
     sbc a,(iy+5)
     sbc a,(iy-3)
     sbc a,iyh
     sbc a,iyl
     and b
     and c
     and d
     and e
     and h
     and l
     and (hl)
     and a
     and 0x2a
     and (ix+5)
     and (ix-3)
     and ixh
     and ixl
     and (iy+5)
     and (iy-3)
     and iyh
     and iyl
     xor b
     xor c
     xor d
     xor e
     xor h
     xor l
     xor (hl)
     xor a
     xor 0x2a
     xor (ix+5)
     xor (ix-3)
     xor ixh
     xor ixl
     xor (iy+5)
     xor (iy-3)
     xor iyh
     xor iyl
     or b
     or c
     or d
     or e
     or h
     or l
     or (hl)
     or a
     or 0x2a
     or (ix+5)
     or (ix-3)
     or ixh
     or ixl
     or (iy+5)
     or (iy-3)
     or iyh
     or iyl
     cp b
     cp c
     cp d
     cp e
     cp h
     cp l
     cp (hl)
     cp a
     cp 0x2a
     cp (ix+5)
     cp (ix-3)
     cp ixh
     cp ixl
     cp (iy+5)
     cp (iy-3)
     cp iyh
     cp iyl
     inc b
     inc c
     inc d
     inc e
     inc h
     inc l
     inc (hl)
     inc a
     inc bc
     inc de
     inc hl
     inc sp
     inc ix
     inc (ix+1)
     inc ixh
     inc ixl
     inc iy
     inc (iy+1)
     inc iyh
     inc iyl
     dec b
     dec c
     dec d
     dec e
     dec h
     dec l
     dec (hl)
     dec a
     dec bc
     dec de
     dec hl
     dec sp
     dec ix
     dec (ix+1)
     dec ixh
     dec ixl
     dec iy
     dec (iy+1)
     dec iyh
     dec iyl
     add hl,bc
     adc hl,bc
     sbc hl,bc
     add hl,de
     adc hl,de
     sbc hl,de
     add hl,hl
     adc hl,hl
     sbc hl,hl
     add hl,sp
     adc hl,sp
     sbc hl,sp
     add ix,bc
     add ix,de
     add ix,sp
     add ix,ix
     add iy,bc
     add iy,de
     add iy,sp
     add iy,iy
     rlc b
     rlc c
     rlc d
     rlc e
     rlc h
     rlc l
     rlc (hl)
     rlc a
     rlc (ix+2)
     rlc (iy+2)
     rrc b
     rrc c
     rrc d
     rrc e
     rrc h
     rrc l
     rrc (hl)
     rrc a
     rrc (ix+2)
     rrc (iy+2)
     rl b
     rl c
     rl d
     rl e
     rl h
     rl l
     rl (hl)
     rl a
     rl (ix+2)
     rl (iy+2)
     rr b
     rr c
     rr d
     rr e
     rr h
     rr l
     rr (hl)
     rr a
     rr (ix+2)
     rr (iy+2)
     sla b
     sla c
     sla d
     sla e
     sla h
     sla l
     sla (hl)
     sla a
     sla (ix+2)
     sla (iy+2)
     sra b
     sra c
     sra d
     sra e
     sra h
     sra l
     sra (hl)
     sra a
     sra (ix+2)
     sra (iy+2)
     sll b
     sll c
     sll d
     sll e
     sll h
     sll l
     sll (hl)
     sll a
     sll (ix+2)
     sll (iy+2)
     srl b
     srl c
     srl d
     srl e
     srl h
     srl l
     srl (hl)
     srl a
     srl (ix+2)
     srl (iy+2)
     bit 0,b
     bit 0,c
     bit 0,d
     bit 0,e
     bit 0,h
     bit 0,l
     bit 0,(hl)
     bit 0,a
     bit 0,(ix+4)
     bit 0,(iy+4)
     bit 1,b
     bit 1,c
     bit 1,d
     bit 1,e
     bit 1,h
     bit 1,l
     bit 1,(hl)
     bit 1,a
     bit 1,(ix+4)
     bit 1,(iy+4)
     bit 2,b
     bit 2,c
     bit 2,d
     bit 2,e
     bit 2,h
     bit 2,l
     bit 2,(hl)
     bit 2,a
     bit 2,(ix+4)
     bit 2,(iy+4)
     bit 3,b
     bit 3,c
     bit 3,d
     bit 3,e
     bit 3,h
     bit 3,l
     bit 3,(hl)
     bit 3,a
     bit 3,(ix+4)
     bit 3,(iy+4)
     bit 4,b
     bit 4,c
     bit 4,d
     bit 4,e
     bit 4,h
     bit 4,l
     bit 4,(hl)
     bit 4,a
     bit 4,(ix+4)
     bit 4,(iy+4)
     bit 5,b
     bit 5,c
     bit 5,d
     bit 5,e
     bit 5,h
     bit 5,l
     bit 5,(hl)
     bit 5,a
     bit 5,(ix+4)
     bit 5,(iy+4)
     bit 6,b
     bit 6,c
     bit 6,d
     bit 6,e
     bit 6,h
     bit 6,l
     bit 6,(hl)
     bit 6,a
     bit 6,(ix+4)
     bit 6,(iy+4)
     bit 7,b
     bit 7,c
     bit 7,d
     bit 7,e
     bit 7,h
     bit 7,l
     bit 7,(hl)
     bit 7,a
     bit 7,(ix+4)
     bit 7,(iy+4)
     res 0,b
     res 0,c
     res 0,d
     res 0,e
     res 0,h
     res 0,l
     res 0,(hl)
     res 0,a
     res 0,(ix+4)
     res 0,(iy+4)
     res 1,b
     res 1,c
     res 1,d
     res 1,e
     res 1,h
     res 1,l
     res 1,(hl)
     res 1,a
     res 1,(ix+4)
     res 1,(iy+4)
     res 2,b
     res 2,c
     res 2,d
     res 2,e
     res 2,h
     res 2,l
     res 2,(hl)
     res 2,a
     res 2,(ix+4)
     res 2,(iy+4)
     res 3,b
     res 3,c
     res 3,d
     res 3,e
     res 3,h
     res 3,l
     res 3,(hl)
     res 3,a
     res 3,(ix+4)
     res 3,(iy+4)
     res 4,b
     res 4,c
     res 4,d
     res 4,e
     res 4,h
     res 4,l
     res 4,(hl)
     res 4,a
     res 4,(ix+4)
     res 4,(iy+4)
     res 5,b
     res 5,c
     res 5,d
     res 5,e
     res 5,h
     res 5,l
     res 5,(hl)
     res 5,a
     res 5,(ix+4)
     res 5,(iy+4)
     res 6,b
     res 6,c
     res 6,d
     res 6,e
     res 6,h
     res 6,l
     res 6,(hl)
     res 6,a
     res 6,(ix+4)
     res 6,(iy+4)
     res 7,b
     res 7,c
     res 7,d
     res 7,e
     res 7,h
     res 7,l
     res 7,(hl)
     res 7,a
     res 7,(ix+4)
     res 7,(iy+4)
     set 0,b
     set 0,c
     set 0,d
     set 0,e
     set 0,h
     set 0,l
     set 0,(hl)
     set 0,a
     set 0,(ix+4)
     set 0,(iy+4)
     set 1,b
     set 1,c
     set 1,d
     set 1,e
     set 1,h
     set 1,l
     set 1,(hl)
     set 1,a
     set 1,(ix+4)
     set 1,(iy+4)
     set 2,b
     set 2,c
     set 2,d
     set 2,e
     set 2,h
     set 2,l
     set 2,(hl)
     set 2,a
     set 2,(ix+4)
     set 2,(iy+4)
     set 3,b
     set 3,c
     set 3,d
     set 3,e
     set 3,h
     set 3,l
     set 3,(hl)
     set 3,a
     set 3,(ix+4)
     set 3,(iy+4)
     set 4,b
     set 4,c
     set 4,d
     set 4,e
     set 4,h
     set 4,l
     set 4,(hl)
     set 4,a
     set 4,(ix+4)
     set 4,(iy+4)
     set 5,b
     set 5,c
     set 5,d
     set 5,e
     set 5,h
     set 5,l
     set 5,(hl)
     set 5,a
     set 5,(ix+4)
     set 5,(iy+4)
     set 6,b
     set 6,c
     set 6,d
     set 6,e
     set 6,h
     set 6,l
     set 6,(hl)
     set 6,a
     set 6,(ix+4)
     set 6,(iy+4)
     set 7,b
     set 7,c
     set 7,d
     set 7,e
     set 7,h
     set 7,l
     set 7,(hl)
     set 7,a
     set 7,(ix+4)
     set 7,(iy+4)
     jp nn
     jp nz,nn
     jp z,nn
     jp nc,nn
     jp c,nn
     jp po,nn
     jp pe,nn
     jp p,nn
     jp m,nn
     jp (hl)
     jp (ix)
     jp (iy)
     call nn
     call nz,nn
     call z,nn
     call nc,nn
     call c,nn
     call po,nn
     call pe,nn
     call p,nn
     call m,nn
     ret
     ret nz
     ret z
     ret nc
     ret c
     ret po
     ret pe
     ret p
     ret m
     rst 0x0
     rst 0x8
     rst 0x10
     rst 0x18
     rst 0x20
     rst 0x28
     rst 0x30
     rst 0x38
     jr fwd
     jr nz,fwd
     jr z,fwd
     jr nc,fwd
     jr c,fwd
     djnz fwd
     defs 20
fwd: nop
     jr fwd
     in a,(0x40)
     out (0x40),a
     in b,(c)
     in c,(c)
     in d,(c)
     in e,(c)
     in h,(c)
     in l,(c)
     in a,(c)
     in f,(c)
     out (c),b
     out (c),c
     out (c),d
     out (c),e
     out (c),h
     out (c),l
     out (c),a
     out (c),0
     im 0
     im 1
     im 2
     db 0x01, 0x02, 0xff, 65, "AB"
     dw 0x1234, nn
     defs 4, 0xaa
done: nop
